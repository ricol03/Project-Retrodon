#include <windows.h>
#include <winuser.h>
#include <commctrl.h>

#ifndef CONTROLS_H_
#define CONTROLS_H_

int homeWindow(HWND hwnd);
int instanceWindow(HWND hwnd);
int accountWindow(HWND hwnd);

#endif